[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "typamud"
version = "0.1.0"
description = "Minimal asyncio telnet MUD MVP"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
